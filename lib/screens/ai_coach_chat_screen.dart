import 'package:flutter/material.dart';

class AICoachChatScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('AI Coach Chat'),
      ),
      body: Center(
        child: Text('AI Coach Chat Screen'),
      ),
    );
  }
}
